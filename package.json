{
  "name": "uplus",
  "version": "1.1.0",
  "description": "Enhance your Udemy learning with progress tracking and video controls.",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "format": "prettier --write scripts/**/*.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.5",
    "prettier": "^3.5.3"
  }
}